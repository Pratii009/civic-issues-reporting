<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F6F8FB"
    android:fillViewport="true"
    tools:context=".NotificationFragment">

    <LinearLayout
        android:id="@+id/notificationContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Notifications"
            android:textSize="24sp"
            android:textStyle="bold"
            android:layout_marginBottom="16dp"
            android:textColor="#22335A" />

        <!-- Example notification card; cards to be added dynamically in Java code -->
        <!-- You can use this template for each notification in a RecyclerView or directly add views -->

        <LinearLayout
            android:id="@+id/notificationCardExample"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@drawable/card_bg"
            android:padding="28dp"
            android:layout_marginBottom="16dp"
            android:elevation="6dp">

            <!-- Status row -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <ImageView
                    android:id="@+id/statusIcon"
                    android:layout_width="32dp"
                    android:layout_height="32dp"
                    android:src="@drawable/ic_check_circle"
                    android:layout_marginRight="10dp" />

                <TextView
                    android:id="@+id/statusText"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Solved"
                    android:textSize="17sp"
                    android:textStyle="bold"
                    android:textColor="#30C97D" />
            </LinearLayout>

            <!-- Title -->
            <TextView
                android:id="@+id/notificationTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Pothole"
                android:textSize="20sp"
                android:textStyle="bold"
                android:textColor="#22335A"
                android:layout_marginTop="18dp"
                android:layout_marginBottom="6dp" />

            <!-- Description/body -->
            <TextView
                android:id="@+id/notificationBody"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="There is a pothole at the intersection of 5th Ave and 7th Street."
                android:textSize="15sp"
                android:textColor="#656D7B"
                android:layout_marginBottom="12dp" />

            <!-- Status update message -->
            <TextView
                android:id="@+id/notificationMessage"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Your reported issue has been resolved by the admin."
                android:textSize="15sp"
                android:textColor="#656D7B" />
        </LinearLayout>
    </LinearLayout>
</ScrollView>
